version: "3.9"

services:
  truetraffic:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - AGENTS_PATH=/app/data/ai-agents.json
    restart: unless-stopped
