[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["checker/tests"]
asyncio_mode = "auto"

[tool.ruff.lint.isort]
known-first-party = ["checker"]
